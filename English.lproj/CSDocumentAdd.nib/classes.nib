{
    IBClasses = (
        {
            ACTIONS = {add = id; clear = id; }; 
            CLASS = CSWinCtrlAdd; 
            LANGUAGE = ObjC; 
            SUPERCLASS = CSWinCtrlEntry; 
        }, 
        {
            ACTIONS = {generateRandomPassword = id; openURL = id; }; 
            CLASS = CSWinCtrlEntry; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                accountText = NSTextField; 
                category = NSComboBox; 
                mainButton = NSButton; 
                nameText = NSTextField; 
                notes = NSTextView; 
                passwordText = NSTextField; 
                urlText = NSTextField; 
            }; 
            SUPERCLASS = NSWindowController; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}